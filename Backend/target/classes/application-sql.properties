# ── Perfil SQL (activo por defecto) ──────────────────────────────────────────

# ── MongoDB (deshabilitado temporalmente — descomentar para activar) ──────────
# spring.data.mongodb.uri=mongodb://localhost:27017/thinwallet_db
# spring.data.mongodb.database=thinwallet_db
# spring.data.mongodb.auto-index-creation=true
# mongodb.enabled=true

# Excluir autoconfiguraciones de MongoDB para que Spring no intente conectarse
spring.autoconfigure.exclude=\
  org.springframework.boot.autoconfigure.mongo.MongoAutoConfiguration,\
  org.springframework.boot.autoconfigure.data.mongo.MongoDataAutoConfiguration,\
  org.springframework.boot.autoconfigure.data.mongo.MongoRepositoriesAutoConfiguration
